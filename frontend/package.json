{
  "name": "frontend",
  "version": "0.0.0",
  "workspaces": [
    "packages/*",
    "client"
  ],
  "scripts": {
    "build": "nx build",
    "graph": "nx run-many --target=build --graph"
  },
  "packageManager": "yarn@4.3.1",
  "devDependencies": {
    "nx": "^19.4.1",
    "typescript": "^5"
  }
}
